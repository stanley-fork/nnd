lazy formatting of cells;
line wrapping;
caching wrapped cell heights across frames;
scroll within cell if it's taller than area;
horizontal scrolling;
cutoff indication ("..." if unscrollable, "\\" if wrapped, "<"/">" if scrollable);
continuity of scroll when cell heights change (e.g. watch values become unknown);
multiline text editing;

struct Table {
    
}

struct Row {
    
}

struct Cell {
    
}
